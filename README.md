# Video Downloader GUI
Một công cụ tải video với giao diện trực quan, hoạt động độc lập không cần cài đặt phức tạp.

## ✨ Tính Năng Nổi Bật

- **Tải Nhiều Video Cùng Lúc**: Hỗ trợ nhập danh sách link (mỗi dòng một link) và tải đa luồng để tiết kiệm thời gian.
- **Tuỳ Chọn Chất Lượng Video**: Chọn giới hạn chất lượng linh hoạt từ 360p, 480p, 720p, 1080p, 1440p (2K) tới 2160p (4K).
- **Trình Quản Lý Dependencies Tự Động**: 🖥️ Tool sẽ tự động tìm và tải các bộ công cụ phụ trợ còn thiếu (`yt-dlp`, `ffmpeg`, `deno`) ngay lần chạy đầu tiên.
- **Tự Động Cập Nhật**: Tự động lấy các bản vá mới nhất của `yt-dlp` để việc tải video từ YouTube ổn định.
- **Tự Động Xin Cấp Quyền Vượt Bot**: Nếu video bị chặn bởi captcha/bot-checker của YouTube, công cụ sẽ tự động xuất cookie từ trình duyệt Firefox nội bộ của bạn để tải vượt rào an toàn.
- **Giao Diện Trực Quan**: Sử dụng thư viện `customtkinter` tạo ra giao diện Dark/Light mode hiện đại, có cả một màn hình Console Log hỗ trợ các mã màu báo lỗi/báo thành công ngay bên trong.

## 🚀 Hướng Dẫn Sử Dụng

1. Tải ứng dụng [tại đây](https://github.com/DevNew440/VideoDownloader/blob/main/VideoDownloader.exe) (Bấm nút Download raw để tải về).
2. Mở tệp `VideoDownloader.exe`.
3. Nhập từ một tới nhiều link YouTube/TikTok vào ô danh sách link.
4. Lựa chọn các thông số về Số Luồng, Chất Lượng Video.
5. Cuối cùng, nhấn vào **TẢI VIDEO** và tận hưởng!



